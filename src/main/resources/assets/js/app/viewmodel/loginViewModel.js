define([
        'jquery',
        'underscore',
        'knockout',
        'app/service/http'
], function($, _, ko, http) {	
	var appViewModel = function(options) {
		
	}
	
	return appViewModel;
});